/* Write two binary functions, add and mul, that take two numbers and return their sum and product

 

add(3, 4)  // return 7

mul(3, 4)  // return 12 */

function add(a,b){
    return (a+b);
}

function mul(a,b){
    return (a*b);
}

console.log(add(3,4));
console.log(mul(3,4));
